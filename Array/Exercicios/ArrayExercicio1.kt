//Declaração da função main
fun main() {

// Vamos supor que temos o seguinte array de numeros
val numeros = arrayOf(10,20,30,40,50)

// Vamos usar um loop para imprimir cada número de array
for(numero in numeros) {

    println("Numero: $numero")
    
   }
}